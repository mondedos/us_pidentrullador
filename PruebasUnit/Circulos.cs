using System;
using System.Collections.Generic;
using System.Text;
using kernel;
using NUnit.Framework;

namespace PruebasUnit
{
    [TestFixture]
    public class Circulos
    {
        /*
        Circulo c;
        [TestFixtureSetUp]
        public void SettingUp()
        {
            //Esto se ejecuta al iniciarse todos los test unitarios
            Atributos atr = Atributos.getInstance();

            atr.radiosL = new int[] { 27, 45, 63, 81 };
            atr.puntosK = new int[] { 10, 16, 22, 28 };
            Minucia m=new Minucia();
             c= new Circulo(m,1);
            
            c.puntos[0] = new Punto();
        }

        [TestFixtureTearDown]
        public void TearDowning()
        {
            //Esto se ejecuta al cerrarse todos los test unitarios
        }

        [Test]
        public void get_fi_polar_punto0()
        {
            Assert.IsTrue(c.get_fi_polar_punto(0) == 0);
        }
        [Test]
        public void get_fi_polar_punto1()
        {
            Assert.IsFalse(c.get_fi_polar_punto(1) == 0);
        }
        */
    }
}
